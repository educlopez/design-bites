# liam.is — Design System

## 1. Visual Theme & Atmosphere

liam.is is a study in typographic contradiction: the body is set in Times (the system serif), while headings use Inter (the ubiquitous geometric sans). This inversion of the conventional hierarchy — where headings are usually the display face and body the workhorse — creates an unusual reading dynamic. The result feels like a personal site written in a notebook, where the prose is intimate and the labels are clinical. At 12px throughout, the entire site operates at a micro-scale, creating a dense, almost zine-like quality. The palette is black on white with a single electric blue accent (`#0099ff`) and secondary grey (`#888`). The atmosphere is minimal, personal, and slightly anti-corporate.

## 2. Color Palette & Roles

| Role | Value | Usage |
|---|---|---|
| Background | `#ffffff` | Page canvas |
| Primary text | `#333333` | Headings, main content |
| Secondary text | `#888888` | Metadata, captions, secondary labels |
| Accent / tint | `#0099ff` | Links, interactive highlights |
| Body text | `#000000` | Raw body default |

The palette is a three-step greyscale (black → `#333` → `#888`) with a single bright accent. The accent blue is vivid against the neutral field — it reads as intentional and brand-like despite the overall minimalism. Secondary grey `#888` is used to visually recede metadata without hiding it.

## 3. Typography Rules

**Body typeface:** Times (system serif), 12px, weight 400  
**Heading typeface:** Inter, sans-serif, 12px, weight 700

- **H1:** Inter, 12px, bold — same size as body, differentiated only by weight and typeface
- **Body:** Times, 12px, regular — the serif as the reading face
- No line-height customisation extracted — defaults to browser normal (~1.2)
- No letter-spacing, no text transforms
- Font feature settings include standard ligatures (`clig`, `liga`)

The decision to use the same 12px size across headings and body means hierarchy is established entirely through weight contrast (700 vs 400) and the serif/sans opposition, not size. This is typographically sophisticated in its restraint.

## 4. Component Stylings

- **Headings:** Inter bold, 12px — visually compact, label-like
- **Body text:** Times, 12px — classic editorial serif at micro scale
- **Links:** Likely `#0099ff` tint colour (defined in custom props)
- **Borders / shadows:** None extracted — not used
- **Border-radius:** None extracted — likely 0
- **Surfaces:** No extracted surface styles — pure white throughout

## 5. Layout Principles

Spacing is minimal — body padding and margin are 0, suggesting the layout is built through typographic rhythm alone. No grid system, no flexbox gaps extracted. The layout likely follows document flow with modest paragraph spacing. The micro-scale (12px) means the page can hold significant content density without feeling cluttered.

## 6. Depth & Elevation

No shadows, no surfaces, no elevation system. The design is completely flat. Hierarchy is expressed through typography alone: typeface choice, weight, and the `#888` secondary colour. There is no Z-axis in this design system.

## 7. Do's and Don'ts

**Do:**
- Use Times for body prose — the serif creates warmth at small sizes
- Use Inter bold for labels and headings — the contrast with Times creates structure
- Keep everything at 12px — size uniformity is the point
- Use `#0099ff` sparingly as the one accent
- Let `#888` carry secondary information (dates, tags, metadata)

**Don't:**
- Scale up font sizes for headings — the uniform size is intentional
- Add shadows or surface backgrounds
- Introduce additional typefaces or weights
- Use colour beyond the three-step greyscale + single accent
- Add decorative borders or dividers

## 8. Responsive Behavior

No breakpoints extracted. The single-column, document-flow layout adapts naturally to all viewports. At 12px, the content density is consistent across screen sizes. The design likely adds modest horizontal padding at mobile widths.

## 9. Agent Prompt Guide

> Replicate liam.is: white background, Times (system serif) for body at 12px/regular, Inter for headings at 12px/bold — same size, hierarchy by typeface and weight only. Three-step greyscale: `#000` body, `#333` headings, `#888` secondary. Single accent: `#0099ff`. No shadows, no radius, no surfaces. Flat document flow. Dense, personal, micro-scale aesthetic.

---
*Generated by Sparkbites — extracted from live CSS analysis*
